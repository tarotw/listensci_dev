name: Generate static index2

on:
  push:
    paths:
      - 'index.html'
      - 'index2.html'
      - 'soundon.xml'
      - 'apple.json'
      - 'scripts/**'
      - '.github/workflows/generate-index2.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'index2.html'
      - 'soundon.xml'
      - 'apple.json'
      - 'scripts/**'
      - '.github/workflows/generate-index2.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Generate index2.html
        run: python scripts/generate_index2.py
      - name: Verify generated file is up to date
        run: |
          if ! git diff --exit-code index2.html; then
            echo 'index2.html is not up to date. Run python scripts/generate_index2.py and commit the result.' >&2
            exit 1
          fi
